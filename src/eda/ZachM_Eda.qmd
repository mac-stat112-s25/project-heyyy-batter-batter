---
title: "Zach M"
format: html
---

```{r}
#load packages
library(tidyverse)
library(baseballr)
```

```{r}
Park_data_24 <- fg_park(2024)
```

```{r}
# view basics of data
summary(Park_data_24) #get summary of values of each variable

head(Park_data_24) #view what the data looks like

```

```{r}
ggplot(Park_data_24, aes(x = hr, y = basic_5yr)) +
   geom_point() +
  geom_text(aes(label = home_team), angle = 360, vjust = 0, nudge_y = - 0.5)
```

```{r}
ballparks_stadium_data <- read_csv("~/Documents/GitHub/project-heyyy-batter-batter/data/ballparks_stadium_data.csv")
head(ballparks_stadium_data)
```
```{r}
ggplot(ballparks_stadium_data, aes(x = center_field, y = hr_park_effects)) +
  geom_point()+
  geom_smooth(method = "lm")
```


